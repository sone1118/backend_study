mixin video(video)
    div 
        h4
            a(href=`/videos/${video._id}`)=video.title
        p=video.description
        ul 
            each hashtag in video.hashtags
                li=hashtag
            else
                li no hash

        small=video.createAt
        hr
