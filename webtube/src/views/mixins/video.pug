mixin video(info)
    div 
        h3=info.title
        ul 
            li #{info.rating}/5
            li #{info.comments} comments
            li Posted #{info.createAt}
            li #{info.views} views